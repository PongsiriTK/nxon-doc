version: 1
frontend:
  phases:
    preBuild:
      commands:
        - pip3 install -r requirements.txt
    build:
      commands:
        - mkdocs build
  artifacts:
    baseDirectory: public
    files:
      - '**/*'
  cache:
    paths:
      - .cache/**/*
